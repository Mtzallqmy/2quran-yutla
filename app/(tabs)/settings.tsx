import MaterialCommunityIcons from "@expo/vector-icons/MaterialCommunityIcons";
import { Pressable, ScrollView, StyleSheet, Switch, Text, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { haptic } from "@/lib/haptics";
import { useQuranTheme } from "@/lib/quran-theme";

export default function SettingsScreen() { const { colors, mode, toggleTheme } = useQuranTheme(); const entries = [["اللغة", "العربية", "translate"], ["مؤقت النوم", "من المشغل الكامل", "clock-outline"], ["المصادر", "بيانات محلية قابلة للتحديث", "database-outline"]] as const; return <ScreenContainer style={{ backgroundColor: colors.background }}><ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}><Text style={[styles.title, { color: colors.text }]}>إعدادات</Text><Text style={[styles.subtitle, { color: colors.textMuted }]}>اضبط تجربتك بالطريقة التي تناسبك</Text><Text style={[styles.group, { color: colors.gold }]}>المظهر</Text><View style={[styles.row, { backgroundColor: colors.surface, borderColor: colors.border }]}><Switch value={mode === "dark"} onValueChange={() => { haptic.medium(); toggleTheme(); }} trackColor={{ false: colors.border, true: colors.emerald }} /><View style={styles.copy}><Text style={[styles.rowTitle, { color: colors.text }]}>الوضع الداكن</Text><Text style={[styles.rowSub, { color: colors.textMuted }]}>راحة أكثر في الاستماع الليلي</Text></View><MaterialCommunityIcons name="weather-night" size={22} color={colors.gold} /></View><Text style={[styles.group, { color: colors.gold }]}>التجربة</Text>{entries.map(([title, sub, icon]) => <Pressable key={title} style={({ pressed }) => [styles.row, { backgroundColor: colors.surface, borderColor: colors.border }, pressed && styles.pressed]}><MaterialCommunityIcons name={icon} size={22} color={colors.emerald} /><View style={styles.copy}><Text style={[styles.rowTitle, { color: colors.text }]}>{title}</Text><Text style={[styles.rowSub, { color: colors.textMuted }]}>{sub}</Text></View><MaterialCommunityIcons name="chevron-left" size={22} color={colors.textMuted} /></Pressable>)}<View style={[styles.about, { backgroundColor: colors.emeraldSoft }]}><Text style={[styles.aboutTitle, { color: colors.emerald }]}>قرآن يتلى</Text><Text style={[styles.aboutText, { color: colors.textMuted }]}>صُمم بعناية لتكون التلاوة أقرب إليك، في أي وقت.</Text><Text style={[styles.aboutBy, { color: colors.gold }]}>من تطوير معتز العلقمي</Text></View></ScrollView></ScreenContainer>; }
const styles = StyleSheet.create({ content: { padding: 18, paddingBottom: 176 }, title: { textAlign: "right", fontWeight: "900", fontSize: 28, lineHeight: 36 }, subtitle: { textAlign: "right", fontSize: 13, marginTop: 4 }, group: { textAlign: "right", fontSize: 11, fontWeight: "900", marginTop: 25, marginBottom: 9 }, row: { minHeight: 72, borderWidth: 1, borderRadius: 20, paddingHorizontal: 14, flexDirection: "row-reverse", alignItems: "center", gap: 11, marginBottom: 8 }, copy: { flex: 1, alignItems: "flex-end" }, rowTitle: { fontSize: 14, fontWeight: "800" }, rowSub: { fontSize: 11, marginTop: 3, textAlign: "right" }, about: { marginTop: 28, borderRadius: 24, padding: 20, alignItems: "flex-end" }, aboutTitle: { fontSize: 18, fontWeight: "900" }, aboutText: { textAlign: "right", marginTop: 5, fontSize: 12, lineHeight: 19 }, aboutBy: { fontSize: 11, fontWeight: "800", marginTop: 10 }, pressed: { opacity: .75, transform: [{ scale: .98 }] } });
